// Keep Slack prompt copy in one place so all posting paths match.
export function formatThemeMessage(theme) {
  return `:musical_note: Today's music theme: ${theme}\n\nDrop a Spotify link in the thread or channel.`;
}
